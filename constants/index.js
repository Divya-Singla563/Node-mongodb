const { enums } = require("./enum");
const { en } = require("./messages");
const { emailRegex, phoneNoRegex } = require("./regex");

module.exports = {
  emailRegex,
  phoneNoRegex,
  enums,
  en,
};
